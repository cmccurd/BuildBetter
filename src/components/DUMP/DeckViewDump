import React, { useState } from 'react';

const DeckView = ({ deckList }) => {

  const [untagged, untag] = useState(0);
  const [ramp, setRamp] = useState(0);
  const [cardDraw, setCardDraw] = useState(0);
  const [singleTargetRemoval, setSingleTargetRemoval] = useState(0);
  const [boardWipes, setBoardWipes] = useState(0);
  const [lands, setLands] = useState(0);
  const [standalone, setStandalone] = useState(0);
  const [enhancer, setEnhancer] = useState(0);
  const [enabler, setEnabler] = useState(0);

  return (
    <div className="deckview">
      <div className="deckview-columns">

      </div>
    </div>
  );
}

export default DeckView;